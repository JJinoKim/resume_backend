let Koa,Router,bodyParser,mongoose,api;_21d‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_21d‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_21d‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_21d‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_21d‍.w("./api",[["default",["api"],function(v){api=v}]]);require('dotenv').config();







const { PORT, MONGO_URI } = process.env;

mongoose
    .connect(MONGO_URI, { useNewUrlParser: true})
        .then(() => {
            console.log("connected to mongodb");
        })
        .catch((e) => {
            _21d‍.g.console.error(e);
        });


const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes());

// 라우터 적용 전에 bodyparser 적용
app.use(bodyParser());

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
    _21d‍.g.console.log('Listening to port %d', port);
});