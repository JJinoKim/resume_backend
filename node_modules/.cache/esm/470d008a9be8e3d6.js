let Koa,Router,bodyParser,mongoose,jwtMiddleware,api;_9be‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_9be‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_9be‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_9be‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_9be‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);_9be‍.w("./api",[["default",["api"],function(v){api=v}]]);require('dotenv').config();









const { PORT, MONGO_URI } = process.env;

mongoose
    .connect(MONGO_URI, { useNewUrlParser: true})
        .then(() => {
            console.log("connected to mongodb");
        })
        .catch((e) => {
            _9be‍.g.console.error(e);
        });


const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes());

// 라우터 적용 전에 bodyparser 적용
app.use(bodyParser());
app.use(jwtMiddleware);

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
    _9be‍.g.console.log('Listening to port %d', port);
});