let Koa,Router,bodyParser,mongoose,api;_13f‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_13f‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_13f‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_13f‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_13f‍.w("./api",[["default",["api"],function(v){api=v}]]);require('dotenv').config();







const { PORT, MONGO_URI } = process.env;

mongoose
    .connect(MONGO_URI, { useNewUrlParser: true})
        .then(() => {
            console.log("connected to mongodb");
        })
        .catch((e) => {
            _13f‍.g.console.error(e);
        });


const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes());

// 라우터 적용 전에 bodyparser 적용
app.use(bodyParser());

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
    _13f‍.g.console.log('Listening to port %d', port);
});